"""
Application identity and versioning — single source of truth for the GUI.

The engine version mirrors the scanner's own release line (see
``PROJECT_STATE.md``); the GUI version is independent and bumps per UI release.
"""

APP_NAME = "SEA Corporate Security Scanner"
ORGANIZATION = "SEA Corporate"

GUI_VERSION = "2.0.0"
ENGINE_VERSION = "2.0.0"
